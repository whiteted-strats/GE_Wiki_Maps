# Linting for the gaps package: `ruff check gaps` and `ruff format gaps`
[tool.ruff]
line-length = 100
target-version = "py311"
include = ["gaps/**/*.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "N", "RUF"]
